### LambdaEnginePathFilterInput
- prefixInclusions: list of System.Strings
  - List of path prefixes to include, Paths that do not start with a listed prefix are excluded.  If empty, all paths are included.
