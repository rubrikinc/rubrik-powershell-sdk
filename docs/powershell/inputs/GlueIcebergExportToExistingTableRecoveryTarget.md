### GlueIcebergExportToExistingTableRecoveryTarget
Write the snapshot into a different, already-existing Iceberg table.

- destTableId: System.String
  - FID of the destination GlueIcebergTable to write the recovered
snapshot into.
