### CreateVrmInput
Input for adding a FusionCompute Virtual Resource Management (VRM) instance.

- clusterUuid: System.String
  - Required. UUID used to identify the cluster that the request goes to.
- vrmDetail: FusionComputeVrmInput
  - Required. The hostname and account credentials of the FusionCompute VRM instance that is being added.
