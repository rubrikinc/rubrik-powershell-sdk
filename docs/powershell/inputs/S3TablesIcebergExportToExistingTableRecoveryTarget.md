### S3TablesIcebergExportToExistingTableRecoveryTarget
Write the snapshot into a different, already-existing S3 Tables Iceberg
table.

- destTableId: System.String
  - FID of the destination S3 Tables Iceberg table to write the recovered
snapshot into.
