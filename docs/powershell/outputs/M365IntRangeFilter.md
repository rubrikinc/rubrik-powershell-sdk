### M365IntRangeFilter
An integer range with optional lower and upper bounds. At least one bound
must be set.

- minValue: System.Int64
  - Minimum value (inclusive). Omit to set no lower bound.
- maxValue: System.Int64
  - Maximum value (inclusive). Omit to set no upper bound.
