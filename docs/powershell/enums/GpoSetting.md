### GpoSetting
The configuration state of a Group Policy setting within a GPO.

- GPO_SETTING_UNCONFIGURED - The setting is present in the GPO but left unconfigured.
- GPO_SETTING_CONFIGURED - The setting is configured by the GPO.
